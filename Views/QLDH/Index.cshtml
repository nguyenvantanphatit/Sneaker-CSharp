@model IEnumerable<Sneaker.Models.DONDATHANG>

@{ ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml"; }

<h3 style="text-align: center ; color:red;">QUẢN LÝ ĐƠN ĐẶT HÀNG</h3>

@*<p>
        @Html.ActionLink("Tạo Mới", "Create")
    </p>*@
<div class="container">

    <div class="row">

        <div class="col-md-3">

            <div class="list-group">
                <p>
                    @Html.ActionLink("ExportExcel", "ExportExcel", "Excel")
                </p>
            </div>
        </div>

        <div class="col-md-9">

            <div class="row carousel-holder">

                <div class="col-md-12">
                    <table class="table">
                        <tr>
                            <th>
                                HỌ TÊN
                            </th>
                            <th>
                                TÌNH TRẠNG GIAO
                            </th>
                            <th>
                                NGÀY ĐẶT
                            </th>
                            <th>
                                NGÀY GIAO
                            </th>
                            <th>
                                MÃ KHÁCH HÀNG
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.KHACHHANG.HOTEN)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TINHTRANGGIAOHANG)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NGAYDAT)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NGAYGIAO)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MAKH)
                        </td>   
                        <td>
                            @*@Html.ActionLink("Edit", "Edit", new { id=item.MADH }) |*@
                            @Html.ActionLink("Chi Tiết", "Details", new { id = item.MADH }) |
                            @Html.ActionLink("Xóa", "Delete", new { id = item.MADH })
                        </td>
                    </tr>}

                    </table>
                </div>


            </div>



        </div>

    </div>

</div>

